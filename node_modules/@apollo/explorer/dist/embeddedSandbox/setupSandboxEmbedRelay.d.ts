import { HandleRequest } from '../helpers/postMessageRelayHelpers';
export declare function setupSandboxEmbedRelay({ handleRequest, embeddedSandboxIFrameElement, apolloStudioEnv, }: {
    handleRequest: HandleRequest;
    embeddedSandboxIFrameElement: HTMLIFrameElement;
    apolloStudioEnv: 'staging' | 'prod';
}): {
    dispose: () => void;
};
